---
description: 'A Github themed markdown renderer.'
labels: ['markdown', 'github']
---

import Markdown from './markdown';

## React Component for rendering text

A Github themed markdown renderer. Header anchors and clippy for code blocks included.

### Component usage
```js
<Markdown content="<h2>hello from Markdown</h2>" />
```

### Using props to customize the text

Modify the text to see it change live:
```js live
<Markdown content="<h2>hello from Markdown</h2>" />
```
