{
  "name": "components",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm i -g @teambit/bvm && bit install",
    "start": "bit start"
  },
  "devDependencies": {
    "@somarlyonks/tslint": "^1.0.1"
  }
}
